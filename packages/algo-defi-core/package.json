{
  "name": "@blockadjacent/algo-defi-core",
  "version": "1.0.0",
  "description": "Core functions and constants that can be used by other Algorand-based packages in this monorepo.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "compile": "rm -rf dist/* && rm -f tsconfig.pkg.tsbuildinfo && tsc -p tsconfig.pkg.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Owan Hunte <owan@blockadjacent.com>",
  "license": "SEE LICENSE IN <project-root>/LICENSE.md",
  "dependencies": {}
}
